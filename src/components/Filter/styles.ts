import { SxProps, Theme } from '@mui/material';

const variableButton: SxProps<Theme> = (theme) => ({ pl: { xs: 0, sm: 2 }, pt: { xs: 2, sm: 0 } });

const styles = {
  variableButton,
};

export default styles;
